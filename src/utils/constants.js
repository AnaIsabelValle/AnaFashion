
export const API_BASE_URL = "http://localhost:8000/api/v1";

/*
 * This line of code exports a constant named API_BASE_URL,
 * which represents the base URL of the API endpoints used in the web application.
 * In this case, the API base URL is set to "http://localhost:8000/api/v1", 
 * indicating that the API server is hosted locally on the machine where the web application
 * is being developed and that the API version being used is v1.
 * 
*/